---
title: "MTLLFM: Multimodal-Temporal Laughter Localization: UR-FUNNY-Temporal and SMILE-Temporal Benchmarks with an Adaptive Multimodal Fusion Model"
title_zh: MTLLFM：多模态时序笑声定位：含自适应多模态融合模型的UR-FUNNY-Temporal与SMILE-Temporal基准
authors: "Eyal Hanania, Nadav Kirsch, Daniel Arkushin, Jonathan Benvenisti, Amos Bercovich, Elie Zemmour, Sahar Froim"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25409v1"
tags: ["query:vtg"]
score: 10.0
evidence: 视频中笑声事件的精确时间起止边界
tldr: "现有视频笑声检测多采用粗粒度分类，难以定位短暂笑声的精确起止时间，限制了情感计算与叙事理解。为此，我们提出UR-FUNNY-Temporal和SMILE-Temporal两个精细时间标注数据集，涵盖11000多个视频、78.8小时，并标注笑声边界、说话人、模态主导及强度。同时提出MTLLFM轻量级弱监督框架，结合HuBERT和MAE编码器、时序软池化与自适应模态门控，仅从视频级标签学习时态定位。在体育广播数据上F1达99%、定位精度68.1%，超越Gemini 3 Flash等基础模型，且精确时间标签使GPT-3.5在下游笑声推理中超越GPT-4o，CIDEr提升227%，为幽默分析和情感交互提供了新基准。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25409v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 845, \"height\": 1146, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25409v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 870, \"height\": 283, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.25409v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 880, \"height\": 1037, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25409v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1407, \"height\": 304, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25409v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 877, \"height\": 846, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25409v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 870, \"height\": 206, \"label\": \"Table\"}]"
motivation: 现有方法仅粗粒度分类，无法定位笑声精确边界，阻碍了情感计算和叙事理解中瞬态事件的捕捉。
method: 提出MTLLFM轻量级弱监督框架，结合固定HuBERT和MAE编码器、时序软池化与自适应模态门控，仅用视频级标签学习帧级笑声定位。
result: "在三个数据集上，MTLLFM均优于基础模型，尤其在体育广播上F1达99%、定位精度68.1%；精确时间标签使下游笑声推理CIDEr提升227%。"
conclusion: 数据集和MTLLFM模型为视频中笑声的精细时间定位提供了新基准与高效方法，显著提升笑声推理性能，对幽默分析与情感交互有重要价值。
---

## 摘要
视频中的笑声检测对于情感计算和叙事理解至关重要，然而现有方法将其视为粗略的片段级分类，未能捕捉短暂笑声事件的精确时序边界。我们通过两项互补贡献弥补这一差距。首先，我们推出了UR-FUNNY-Temporal和SMILE-Temporal，这是两个全面标注的时序笑声数据集，扩展了两个广泛使用的幽默基准。我们的标注覆盖超过11,053个视频（78.8小时），为每个笑声事件提供精确的起始/结束边界，以及丰富的元数据，区分说话者笑声与观众笑声、模态主导（听觉、视觉或两者兼有）和强度级别。其次，我们提出了一种轻量级弱监督框架用于时序笑声定位。我们的架构结合了固定的HuBERT和MAE编码器，采用时序softmax池化和自适应模态门控，从片段级标签学习细粒度的时序定位，无需在训练期间要求帧级标注。在三个数据集上的实验表明，我们的方法显著优于包括Gemini 3 Flash在内的多模态基础模型，在体育广播数据上实现了99%的F1值和68.1%的定位精度。消融实验验证了每个架构组件的有效性。此外，我们精确的时序标签在下游笑声推理中将CIDEr得分提升了227%，使GPT-3.5超越了GPT-4o。代码、UR-FUNNY-Temporal和SMILE-Temporal数据集已公开于https://github.com/WSCSports/MTLLFM-temporal-laughter-localization。

## Abstract
Detecting laughter in video is essential for affective computing and narrative understanding, yet existing approaches treat it as coarse clip-level classification, failing to capture precise temporal boundaries of brief, transient laughter events. We address this gap with two complementary contributions.   First, we introduce UR-FUNNY-Temporal and SMILE-Temporal, fully annotated temporal laughter datasets extending two widely-used humor benchmarks. Our annotations cover over 11,053 videos (78.8 hours) and provide precise onset/offset boundaries for each laughter event, along with rich metadata distinguishing speaker vs. audience laughter, modality dominance (acoustic, visual, or both), and intensity levels.   Second, we propose a lightweight weakly-supervised framework for temporal laughter localization. Our architecture combines fixed HuBERT and MAE encoders with temporal softmax pooling and adaptive modality gating, learning fine-grained temporal grounding from clip-level labels without requiring frame-level annotations during training. Experiments across three datasets demonstrate that our approach substantially outperforms multimodal foundation models including Gemini 3 Flash, achieving 99% F1 and 68.1% localization precision on sports broadcast data. Ablations validate each architectural component. Furthermore, our precise temporal tags improve downstream laughter reasoning by 227% on CIDEr, enabling GPT-3.5 to outperform GPT-4o. The code, UR-FUNNY-Temporal and SMILE-Temporal datasets are publicly available at https://github.com/WSCSports/MTLLFM-temporal-laughter-localization.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将以 Markdown 格式，对给定的论文《MTLLFM: Multimodal-Temporal Laughter Localization》进行结构化、深入、客观的总结。

### 1. 论文的核心问题与整体含义

*   **核心问题**：现有视频笑声检测方法大多将其视为粗粒度的片段级分类任务，即判断一个长视频片段中“是否包含笑声”。这种方法忽略了笑声事件本身的**瞬态（通常为1-2秒）和稀疏性**特征，无法捕捉笑声的精确起止时间边界，导致了显著的标签噪声和时序表征学习的局限性。
*   **研究动机与背景**：精确的时序定位（即找到笑声“从哪一秒开始，到哪一秒结束”）对情感计算、多模态推理和叙事理解等下游任务至关重要。例如，知道“何时”发笑，对于理解幽默机制、社交动态和上下文线索具有关键的语义价值。因此，该工作旨在弥补从“粗粒度分类”到“细粒度时序定位”之间的研究空白。
*   **整体含义**：本文不仅提出了一个新的弱监督时序定位方法，更构建了首个大规模、精细标注的笑声时序基准数据集。它证明了针对瞬态情感事件进行专门化时序建模的必要性和有效性，并揭示了一个深刻洞见：**精确的时序定位可以赋能更弱的语言模型（GPT-3.5），使其在下游推理任务中超越更强的通用模型（GPT-4o）**。

### 2. 论文提出的方法论

论文提出了一个名为 **MTLLFM** 的轻量级弱监督时序笑声定位框架。其核心思想是仅利用训练时成本更低的**片段级标签**（该片段是否包含笑声），学习出能够进行**帧级时序定位**的注意力机制。

*   **特征提取**：
    *   使用冻结的预训练编码器 **HuBERT-Large** 和 **MAE** 分别提取音频（约50Hz，1024维）和视觉（10fps，768维）特征流，以降低计算成本和训练复杂度。
*   **时序Softmax池化**：
    *   这是一个学习“何处”该关注的关键机制。对于每个模态的特征序列，模型通过可学习的参数计算每个时间步的标量重要性分数，并通过 Softmax 函数将其归一化为一个时序注意力分布 $\alpha$。
    *   该机制采用 **O(T)** 的时间复杂度，远低于自注意力的 **O(T²)**，旨在精准聚焦短暂的情感峰值，并抑制中性背景的干扰。
*   **自适应模态门控**：
    *   为了解决笑声在多模态中表现不对称的问题（如：仅有声音的爆笑 vs. 仅有表情的憋笑），模型从池化后的音频表征 $f_a$ 和视觉表征 $f_v$ 中独立地学习两组门控分数，并通过 Softmax 生成互补的模态权重 $w_a$ 和 $w_v$（和等于 1）。
    *   最终融合表征为 $f_{fused} = w_a \cdot f_a + w_v \cdot f_v$，使模型能够动态地依赖更可靠的模态。
*   **推理与定位**：
    *   模型预测片段是否包含笑声（$\hat{y}$）。对于预测为正的片段，进行后处理定位：
        1.  **温度锐化**：对学习到的注意力分布进行锐化，凸显峰值。
        2.  **对齐与融合**：将音视频注意力对齐到统一的时间分辨率，并用学习的模态权重 $w_a, w_v$ 进行加权组合，得到最终的联合时序信号 $\beta_n$。
        3.  **峰值扩张**：找到联合信号峰值，并向两侧扩张，直到信号值低于均值，扩张后的区间被映射回连续时间戳作为预测的起止边界。

### 3. 实验设计

*   **数据集与场景**：
    *   **UR-FUNNY-Temporal & SMILE-Temporal**：本文自建的新基准，是对现有幽默数据集的时序扩展，提供了精确的笑声起止、说话者、模态主导等标注。涵盖了演讲和脱口秀等对话场景。
    *   **SportsPress**：一个专有的体育广播数据集（新闻发布会、演播室节目），包含3887个5秒片段。该场景的特点是解说员的高能量语音会带来“欺骗性”的噪音，而镜头内的人物可能面无表情，挑战性很高。
*   **对比方法（Benchmark）**：
    *   **多模态基础模型**：Gemini 3 Flash, Qwen2.5 Omni 7B (视频+音频), Qwen2 Audio 7B (纯音频)。这类模型进行零样本推理，直接预测笑声起止时间戳。
    *   **自身变体（消融实验）**：在SportsPress数据集上，对时序池化策略（平均/最大/自注意力池化）、融合策略（拼接/交叉注意力融合）、门控机制（有无Tanh）和单模态模型等进行了详尽的消融对比。
*   **评估指标**：
    *   **分类**：F1 分数。
    *   **定位**：在 IoU=0.5 阈值下的精确率 (Loc@0.5) 和平均 IoU。
    *   **下游应用**：在 Video Laugh Reasoning 任务上，使用 BLEU, METEOR, CIDEr 等文本生成指标评估笑声时序标签对LLM推理能力的增强效果。

### 4. 资源与算力

*   论文**未明确提及**所使用 GPU 的具体型号和数量，也未给出总训练时长。但强调了其架构的“**轻量级**”，使用冻结预训练编码器、线性投影和 O(T) 复杂度的池化机制，暗示了其训练成本远低于需要全量微调或具有 O(T²) 复杂度的基础模型。

### 5. 实验数量与充分性

*   **实验数量与分组**：
    1.  **主对比实验（1组）**：在 **3个** 不同特征的数据集上与 **3个** 强大的多模态基础模型进行对比，评估分类和定位性能。
    2.  **消融实验（1组）**：在 SportsPress 数据集上设计并对比了 **10种** 不同的模型变体，系统性地验证了软池化、Tanh门控、自适应融合等每个核心组件的贡献。
    3.  **下游应用实验（1组）**：在 SMILE 数据集上，用 **2种** LLM（GPT-3.5, GPT-4o）在有无时序标签的条件下进行对比，评估定位技术对高层语义任务的价值。
*   **充分性与客观性**：实验设计非常**充分且客观**。对比基准既包括了任务特定的消融变体，也引入了代表当前最高水平的多模态基础模型，形成了多层次、多维度的对比。评估指标兼顾了分类、定位和下游应用，全面衡量了方法的效果。所有对比均在统一的数据和评估标准下进行，保证了公平性。

### 6. 论文的主要结论与发现

1.  **专业化模型在时序定位上超越通用基础模型**：MTLLFM 在笑声分类和定位上，尤其是定位精度，普遍优于 Gemini 3 Flash 等通用模型。后者虽有强大的语义推理能力，但缺乏处理瞬态事件的细粒度时序精度。
2.  **每个架构组件均不可或缺**：消融实验证实，时序软池化、Tanh门控、自适应模态融合等都带来了可度量的性能增益，证明了该框架针对瞬态情感信号进行了有效优化。
3.  **精确时序定位赋能高层语义推理**：最关键的洞见之一是，仅仅为视频片段添加由模型预测的精确 `<LAUGHTER>` 时间戳，就能让 **GPT-3.5 在笑声推理任务上超越 GPT-4o**（CIDEr提升227%）。这表明，**任务特定的时序定位是一种替代单纯扩大模型规模以解决复杂推理任务的有效、甚至更优的路径**。

### 7. 优点

*   **问题定义明确**：将笑声检测从分类重新定义为更有价值的弱监督时序定位问题，填补了领域空白。
*   **方法论简洁高效**：提出的框架是轻量级的，不依赖帧级标注，计算效率高，设计精巧（如 Tanh 门控防止注意力饱和），具有很强的实用性。
*   **数据集贡献重大**：UR-FUNNY-Temporal 和 SMILE-Temporal 是大规模（>11k视频，78.8小时）、精细标注（起止时间、说话人、模态主导、强度）的基准，为社区后续研究提供了宝贵资源。
*   **实验验证深刻**：不仅验证了定位精度，更通过下游推理实验揭示了时序定位对高层语义理解的巨大杠杆作用，这个发现极具洞察力。

### 8. 不足与局限

*   **算力描述缺失**：未提供具体的硬件配置和训练时长，使得读者难以精确复现或评估其在实际部署中的资源消耗。
*   **数据集可访问性**：专有的 SportsPress 数据集因版权问题无法公开，尽管这是合理的，但使得该领域的实验结果无法完全被复现。
*   **实验覆盖范围**：下游应用实验的样本量相对较小（N=50），且仅验证了字幕增强这一种形式。其对其他任务（如视频剪辑、说话人日志）的影响有待探索。
*   **应用限制**：该方法的性能依赖于音频和视觉模态的可用性与质量。在极端的单模态缺失或严重噪声（如完全遮挡面部、极度嘈杂环境）场景下的鲁棒性未作深入探讨。此外，后处理定位依赖阈值设定，可能在更复杂的多笑声事件场景中面临挑战。

（完）
