---
title: "EventShiftFlow: Towards Hardware-efficient FPGA-based Flow Estimation"
title_zh: EventShiftFlow：面向硬件高效FPGA的流估计
authors: "Arianna Alonso Bizzi, Fernando Cladera, C. J. Taylor"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.28312v1"
tags: ["query:vtg"]
score: 9.0
evidence: 面向低延迟机器人感知的流式速度估计器使能流式视频中的实时感知
tldr: "事件相机输出异步高时间分辨率测量，适用于低延迟机器人感知，然而多数运动估计算法计算量大，难以在FPGA硬件上高效实现。本文提出EventShiftFlow，将异步事件离散为固定时长的时间仓，构建1比特空间占用网格，利用移位寄存器、计数器、比较器和LUT映射乘法等固定宽度整数逻辑，并行评估多个速度假设，为每个活动像素输出稀疏量化速度估计，无需帧重建、浮点运算或迭代优化。在合成数据上正确恢复速度幅度与方向，真实序列方向准确率达99.5%，在10-40%事件密度下保持鲁棒，算法存储小于2kB，并在Xilinx Artix-7上实现单轴原型。该方法为资源受限平台提供了一种低延迟、硬件高效的运动估计方案，适合反应式避障等任务。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.28312v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 913, \"height\": 600, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28312v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 670, \"height\": 1516, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28312v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 916, \"height\": 1418, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28312v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 873, \"height\": 440, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28312v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 909, \"height\": 509, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.28312v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 773, \"height\": 295, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.28312v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 852, \"height\": 198, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.28312v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 811, \"height\": 344, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.28312v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 824, \"height\": 280, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.28312v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 852, \"height\": 685, \"label\": \"Table\"}]"
motivation: 事件相机适合低延迟感知，但现有运动估计方法计算量大，难以在FPGA等资源受限硬件上实现。
method: 提出EventShiftFlow，离散事件为固定时长bin，构建1位占用网格，利用整数逻辑（移位寄存器、计数器等）并行评估速度假设，输出稀疏量化速度估计，无浮点运算和迭代优化。
result: "在合成数据上恢复速度幅度与方向，真实序列方向准确率达99.5%，在10-40%事件密度下鲁棒，存储需求<2kB，在Artix-7 FPGA实现单轴原型。"
conclusion: EventShiftFlow为SWaP受限平台提供硬件高效的运动估计方案，适合反应式避障等低延迟任务，具有高方向准确性和低资源占用。
---

## 摘要
基于事件的视觉传感器提供异步、高时间分辨率的测量，对低延迟机器人感知具有吸引力，但许多基于事件的运动估计方法计算量大，难以映射到FPGA硬件。我们提出了一种流式速度估计器，它将异步事件离散化为固定时长的时间区间，构建1位空间占据网格，并仅使用固定宽度整数逻辑——移位寄存器、计数器、比较器和小型LUT映射乘法——并行评估多个速度假设，无需除法器和DSP模块。该方法无需帧重建、浮点运算和迭代优化。它刻意将密集的亚像素光流置换为每个活动像素处的稀疏量化速度估计，适用于尺寸、重量和功耗受限平台上如反应式避障等低延迟任务。在已知真值速度的含噪合成数据上，该方法恢复了幅度和方向，当不同速度的物体相交时幅度估计最具挑战性。在真实事件相机序列上，所有四个评估运动段的方向准确率达到99.5%，在10-40%的占据密度范围内性能保持鲁棒。我们描述了算法的密度依赖行为，给出了参数敏感性分析，表明所提出的数据通路需要少于2 kB的存储，并在低成本Xilinx Artix-7上实现了单轴原型。

## Abstract
Event-based vision sensors offer asynchronous, high-temporal-resolution measurements that are attractive for low-latency robotic perception, but many event-based motion estimation methods are computationally intensive and difficult to map to FPGA hardware. We present a streaming velocity estimator that discretizes asynchronous events into fixed-duration time bins, constructs a 1-bit spatial occupancy grid, and evaluates multiple velocity hypotheses in parallel using only fixed-width integer logic - shift registers, counters, comparators, and small LUT-mapped multiplies - with no dividers and no DSP blocks. It requires no frame reconstruction, no floating-point arithmetic, and no iterative optimization. The method deliberately trades dense sub-pixel optical flow for a sparse, quantized velocity estimate at each active pixel, suited to low-latency tasks such as reactive obstacle avoidance on size-, weight-, and power-constrained platforms. On noisy synthetic data with known ground-truth velocities, the method recovers both magnitude and direction, with magnitude estimates being most challenged when objects of different velocities intersect. On a real event-camera sequence, directional accuracy reaches 99.5% across all four evaluated motion segments, with performance remaining robust across occupancy densities in the 10-40% range. We characterize the algorithm's density-dependent behavior, present a parameter sensitivity analysis, show that the proposed datapath requires less than 2 kB of storage, and implement a single-axis prototype on a low-cost Xilinx Artix-7.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

*   **核心问题**：事件相机提供异步、高时间分辨率的测量，非常适合低延迟机器人感知；然而，主流的基于事件的光流估计算法（如平面拟合、对比度最大化、深度学习方法）普遍依赖浮点运算、迭代优化或大型神经网络，难以在资源极度受限的硬件（尤其是FPGA/ASIC）上高效部署。
*   **整体含义**：本文旨在为尺寸、重量和功耗（SWaP）严重受限的微型机器人平台提供硬件高效的实时运动估计方案。它刻意**放弃稠密、高精度的光流，转而追求稀疏、可量化的速度估计**，以满足反应式避障等低延迟任务的需求，同时将硬件资源需求压缩至最低。

### 2. 论文提出的方法论

*   **核心思想**：将异步事件流转换为固定时长时间仓内的1位空间占用网格，然后通过移位寄存器、计数器等纯整数逻辑，并行评估一组离散的速度假设（即在时空网格中对角线轨迹上的占用重合次数），选出最匹配的量化速度。
*   **关键技术细节**：
    *   **事件离散化**：将事件流按固定时间窗口 $\Delta t$ 分成时间仓，统计每个像素的事件数，通过阈值 $\theta_e$ 生成**1位二进制占用向量**（该像素在此仓内是否活跃）。
    *   **占用网格**：用 $L$ 个最新时间仓的占用向量构成一个 $N_x \times L$ 的1位网格 $G$，更新采用简单的移位寄存器操作。
    *   **假设提取与评分**：对每个活跃像素 $x_0$，测试离散速度假设 $j$（每时间仓跳 $j$ 像素）。沿对角线回溯：$b_j(h) = G[x_0 - j \cdot h, L - h]$。评分函数为：
        *   **原始位计数（popcount）**：$R_j = \sum_{h=1}^{L} b_j(h)$，仅用计数器实现。
        *   **无除法归一化比较**：为避免除法，用交叉乘法比较假设 $j$ 和 $k$：$R_j \cdot H_k > R_k \cdot H_j$，其中 $H$ 为有效步数。
    *   **优胜选择**：取得分最高的假设作为该像素的速度，并施加最低得分阈值 $\theta_s$。
    *   **2D扩展**：$x$ 轴和 $y$ 轴使用独立的相同管道分别处理，最终通过 $x$ 轴检测处 $y$ 方向速度的中值来合成二维速度向量。
    *   **硬件映射**：所有关键操作（移位、计数、比较）均不使用除法器、浮点单元或DSP块。存储需求极低（双轴合计 < 13 kbit）。文中给出了详细的FPGA数据通路架构与延迟估计，并提出了进一步降低延迟的增量评分变体。

### 3. 实验设计

*   **数据集/场景**：
    *   **合成数据**：自建的有噪仿真器，生成多物体（条形、复杂形状）、不同方向与速度的已知真值运动，并加入5%背景噪声事件和时戳抖动。
    *   **真实数据**：RPG事件相机数据集中的 `shapes_rotation` 序列（DAVIS240C传感器，240×180分辨率）。
*   **评估基准（Ground truth）**：
    *   合成数据使用精确的真值速度。
    *   真实数据采用**手动跟踪**提取分段恒定速度作为方向真值；还使用**E2VID重建帧+DIS稠密光流**作为近似真值，两者交叉验证。
*   **对比方法**：
    *   本文定位为极低资源方案，未与高精度光流算法（如E-RAFT、EV-FlowNet）做精度直接对比，而是**在资源维度上与EDFLOW、Aung等人的平面拟合FPGA实现进行了对比**（如存储：本文<2kB vs. EDFLOW的855kB Block RAM；DSP：0 vs. 多少）。

### 4. 资源与算力

*   本文方法**无需训练，完全基于规则**，因此不涉及GPU集群、训练轮数或深度学习算力。
*   **硬件资源**：算法原型在**Xilinx Artix-7 FPGA**（xc7a100tftg256-2）上实现，利用Vivado 2025.2综合布线。整个UART到运动估计的单轴原型消耗13,326个LUT、5,517个寄存器，**零BRAM、零DSP**，功耗估计0.142 W。核心数据通路存储需求 < 2 kB。论文未提及除FPGA外的其他计算资源。

### 5. 实验数量与充分性

*   **实验组数**：
    *   合成数据多场景测试（单物体水平、多滑块、任意角度、复杂形状、相交运动等），验证方向与幅度估计能力。
    *   真实数据 `shapes_rotation` 序列定量评估，分四个运动段给出方向准确率（整体99.5%），并与E2VID+DIS光流方向符号进行定长序列对比。
    *   **参数敏感性分析**：对时间仓长度 $\Delta t$ 和事件阈值 $\theta_e$ 进行网格搜索，绘制方向准确率热力图，揭示了“占据密度10-40%”的理想工作区间。
    *   资源与预估对比表格（表II–V），以及FPGA原型实现结果（表V）。
*   **充分性与客观性**：实验覆盖了合成可控场景和单个真实序列，能够验证算法核心逻辑和硬件可行性。但相比主流光流方法，**缺少在标准光流基准（如DSEC）上与现有方法的定量精度对比**，且真实场景评估较为单一，这限制了其性能边界的全面认知。实验设计相对简洁，侧重于展示资源高效性。

### 6. 论文的主要结论与发现

*   **可行性**：提出的纯整数逻辑、无除法、无浮点运算的流式速度估计器是完全可行的，能够有效检测运动方向，在合成和真实数据上均获得高方向准确率。
*   **硬件高效**：该算法在Xilinx Artix-7上的单轴原型证实了其极端的低资源占用（< 2kB存储，无DSP/BRAM），远低于现有的FPGA光流方案（如EDFLOW）。
*   **鲁棒性**：算法在10–40%的事件占据密度范围内表现稳定，方向准确率高，对参数选择不过分敏感。
*   **局限**：算法牺牲稠密精度和子像素光流，受孔径问题影响，且速度量化为离散层级；参数通用性依赖场景事件率。

### 7. 优点（方法或实验设计上的亮点）

*   **极简硬件映射**：将运动估计彻底转化为移位、计数和比较的组合，完全避免除法、浮点和DSP，对于超低功耗、小封装FPGA极为友好。
*   **算法与硬件协同设计**：从比特向量表示到评分函数的每一种选择（如popcount、交叉乘法归一化）都立足于硬件实现代价的最小化，是一篇典型的算法-硬件协同设计论文。
*   **清晰的硬件分解**：论文给出了详细的FPGA数据通路架构、延迟估计、增量评分变体以及资源表格，使硬件实现的轮廓非常具体。
*   **参数行为刻画**：通过敏感性分析明确划分出最佳占空比工作区间（10–40%），为用户设置参数提供了有价值的指导。

### 8. 不足与局限

*   **精度与适用范围局限**：
    *   **不提供稠密光流**：输出是稀疏、整像素量化的速度，无法替代高精度光流任务。
    *   **孔径问题**：独立1D管道难以处理同轴上多个不同方向物体的复杂情况。
    *   **量化误差**：速度被强制归入离散层级，存在量化误差，无亚像素插值。
*   **实验覆盖不足**：
    *   **缺少标准光流基准测试**：未在DSEC等标准数据集上与主流方法进行端点误差等精度对比，难以客观定位其准确度水平。
    *   **真实场景单一**：仅测试了包含规则几何形状的`shapes_rotation`序列，未在更复杂、动态背景、低纹理的真实场景中验证。
    *   **鲁棒性验证有限**：未系统评估噪声水平、传感器分辨率、物体尺度变化等因素的影响。
*   **参数与通用性**：时间仓长度和阈值依赖手动调节，未提出在线自适应机制；参数对不同场景的泛化能力未经证实。
*   **原型阶段**：当前FPGA实现仅为单轴、串行打分原型，未完全实现双轴并行2D速度合成及增量变体。

（完）
