---
title: "TimeLogic Challenge @ CVPR 2026: Strong MLLMs Meet Evidence-Seeking Agents for Temporal-Logic Video Question Answering"
title_zh: CVPR 2026的TimeLogic挑战：强大多模态大语言模型与面向时序逻辑视频问答的证据寻求智能体
authors: "Zhaoyang Xu, Xusheng He, Wei Liu, Zhenyang Li, Jianlong Wu"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.01631v1"
tags: ["query:vtg"]
score: 9.0
evidence: 时序逻辑问答需要定位动作发生时间并推理先后/重叠等关系
tldr: "时序逻辑视频问答需推理动作间的时间关系，而均匀采样难以捕捉分散或窄边界证据。本文提出证据搜寻代理，通过Think-Act-Observe循环与多粒度采样，关联时间戳使推理转为数值比较，并利用分类器自适应策略与预算。系统零训练耦合Gemini 3.1 Pro，在TimeLogic上达77.13%准确率。这展示主动探索代理在时序推理潜力，为多模态问答提供新范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01631v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1749, \"height\": 420, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01631v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 861, \"height\": 287, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.01631v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 863, \"height\": 232, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01631v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 608, \"height\": 628, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01631v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 863, \"height\": 466, \"label\": \"Table\"}]"
motivation: 标准视觉语言模型采用固定均匀采样，无法精确定位时序逻辑问答中分散或窄边界的时间证据。
method: 证据搜寻代理通过Think-Act-Observe循环，多粒度采样并关联时间戳，将时序推理简化为数值比较，结合轻量分类器定制策略。
result: "在TimeLogic测试集上，零训练系统耦合Gemini 3.1 Pro达到77.13%平均准确率。"
conclusion: 展示了主动探索代理在时序视频推理中的潜力，为多模态时序问答开拓了新范式。
---

## 摘要
时序逻辑视频问答要求模型推理动作之间的相对时间关系，例如之前、之后、直到、自从、重叠以及多事件链，而不仅仅是识别视频中存在什么。标准视觉语言模型通常在固定、均匀采样的帧集上一次性回答此类问题，这与通常局限于狭窄动作边界或分散在多个远隔事件中的证据不匹配。我们提出了一种证据寻求智能体，将时序逻辑视频问答视为主动探索。该智能体遵循由多粒度采样工具包驱动的“思考-行动-观察”循环，其中每次观察都与绝对时间戳交织，从而将时间关系简化为共享时间轴上的数值比较。其行为由基准结构塑造：一个轻量级分类器将每个问题路由到某个时间类别，每个类别都有定制的策略、迭代深度和提示，而采样预算则根据语料库特征和剪辑长度进行调整。最终的无训练系统将Gemini 3.1 Pro与时间推理策略相结合，在官方TimeLogic测试集上实现了77.13%的平均准确率。

## Abstract
Temporal-logic video question answering requires a model to reason about when actions occur relative to one another, such as before, after, until, since, overlap, and multi-event chains, rather than merely what is present in a video. Standard vision-language models typically answer such questions in a single pass over a fixed, uniformly sampled set of frames, which is poorly matched to evidence that is often localized to narrow action boundaries or dispersed across several distant events. We present an evidence-seeking agent that treats temporal-logic VideoQA as active exploration. The agent follows a Think-Act-Observe loop driven by a multi-granular sampling toolkit, where every observation is interleaved with its absolute timestamp so that temporal relations reduce to numerical comparisons on a shared time axis. Its behavior is shaped by benchmark structure: a lightweight classifier routes each question to a temporal category, each with a tailored policy, iteration depth, and prompt, while sampling budgets adapt to corpus characteristics and clip length. The resulting training-free system couples Gemini 3.1 Pro with a temporal-reasoning policy and achieves 77.13 AvgAcc on the official TimeLogic test set.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **核心问题**：现有的视觉语言大模型（VLMs）在处理视频问答时，大多采用对静止帧的均匀采样并一次性给出答案。然而，在时序逻辑视频问答（Temporal-Logic VideoQA）中，答案的证据往往只集中在某些狭窄的动作边界，或分散在多个间隔较远的时刻，均匀采样极易遗漏关键信息。
- **整体含义**：论文指出，时序逻辑问答的关键瓶颈不是模型识别能力不足，而是缺乏主动、精确的证据定位能力。为此，需要将静态的采样问答转变为智能体主动探索证据的过程，从而实现更可靠的时序推理。

### 2. 论文提出的方法论
论文的核心是一个无训练、基于智能体的推理系统，其关键组件如下：

- **证据寻求智能体框架**：
  - 将时序逻辑视频问答建模为“思考-行动-观察”（Think–Act–Observe）循环，类似于 ReAct 模式。智能体并非一次采样即回答，而是动态决定下一步查看视频的哪个部分，积累时间戳和观察记录，最终在完整历史记录基础上推理。
  - 整个过程受基准结构驱动：先用轻量分类器确定问题类别，再据此定制探索策略。

- **多粒度采样工具包**：
  - 提供四种工具，均返回带有绝对时间戳的帧，使时间关系转化为数值比较：
    - `overview`（粗略）：全局均匀采样，了解视频大致结构。
    - `temporal scan`（结构化扫描）：将视频等分并采样，是构建动作时间线的主力工具。
    - `skim`（中粒度）：在指定时间窗口内进行中等密度采样。
    - `focus`（细粒度）：在窄窗口内高密度采样，用于判定动作精确边界。
  - 工具按“粗到细”递进使用，默认以 `temporal scan` 作为入口构建时间线。

- **问题类型感知策略**：
  - 一个简单的基于规则的分类器将问题映射到时间逻辑类别（如 before、after、immediately-after、until、since、三事件链等）。
  - 每种类别配有一套定制策略，包括：初始工具选择、初始帧预算、后续优先工具、最大迭代步数、类型特化推理提示（编码了形式语义和常见陷阱）、思考令牌预算。
  - 对最复杂的多事件链类别，强制要求先输出明确的时间线，再给出答案。

- **自适应预算与时间门控**：
  - 对短视频（低于某个时长阈值）直接单步稠密均匀采样；长视频则采用迭代循环与 `temporal scan` 初始化。
  - 根据源语料库特征（如厨房视频动作密集且无有效音频）调整采样预算、分割粒度、最小步数。
  - 设置最小步骤门控：对于排序和链类问题，防止模型过早输出答案，强制至少进行指定步数的探索。

- **辅助语音上下文**：
  - 对于 CrossTask 语料库，添加 ASR 转录的带时间戳文本，使 VLM 能够将语音叙述与视觉事件对齐；其他语料库则作为纯视觉处理。

### 3. 实验设计
- **数据集与基准**：在官方 TimeLogic 测试集上进行评估，该测试集包含 3,000 个问题，涉及 1,850 个独特视频，来源于四个异构语料库（CrossTask、STAR、AGQA、Breakfast），包含多选题（1,878 个）和判断题（1,122 个）。
- **评价指标**：使用官方 AvgAcc（平均准确率）。
- **对比方法**：该论文作为挑战赛提交，未在文中与其他具体方法进行消融式对比，而是将最终提交结果（77.13 AvgAcc）作为定量结果报告。系统核心对比点在于与标准均匀采样一次性推理的差异（动机部分）。

### 4. 资源与算力
- 论文明确提到 **“无任务特定训练、微调或验证集模型选择”**，所有步骤都在推理时完成。
- **骨干模型**：使用 Gemini 3.1 Pro 作为 VLM 骨干，通过 API 调用。
- **计算资源**：文中未提及 GPU 型号、数量或推理耗时等算力细节。由于系统构建在云端大模型 API 之上，本地主要运算为规则分类与采样调度，对计算资源需求极低。

### 5. 实验数量与充分性
- 实验数量有限：论文仅报告了最终测试集上的单次提交分数（AvgAcc = 77.13），**没有消融实验、不同类别的精度分布、不同采样策略的对比、不同骨干模型的比较或统计显著性检验**。
- 客观性和公平性：作为挑战赛提交，结果在官方测试集上产生，规则分类器和策略基于先验分析确定，未依赖验证集调参，因此避免了数据泄露风险。但由于缺乏内部对比和错误分析，实验的充分性较弱，无法量化各组件（如时间戳交插、分类器、步数门控）的独立贡献。

### 6. 论文的主要结论与发现
- 时序逻辑 VideoQA 的核心在于时间定位：只要将正确帧交给强大的 VLM，模型能够识别动作，但需要专门机制来寻找、排序和验证这些帧。
- 主动证据探索智能体是解决此类问题的有效范式，它将时间关系简化为共享时间轴上的数值比较，无需训练即可显著提升性能。
- 基准结构分析（语料库异构性和长尾复合类别）直接影响方法设计，其中复合三事件链问题最困难，需要时间线优先推理和更深迭代。
- 最终系统结合 Gemini 3.1 Pro 达到 77.13% 准确率，展示了零训练推理代理的潜力。

### 7. 优点
- **方法新颖性**：首次将时序逻辑视频问答定义为主动证据探索问题，并提出完整的 Think–Act–Observe 代理框架。
- **轻量且可移植**：完全免训练，可适配任何帧条件 VLM 骨干，仅需推理时策略调整。
- **设计合理**：通过多粒度工具和时间戳交插，将时间推理变为简单的数值比较；利用规则分类器实现问题特异性适配，对长尾类别有针对性处理。
- **扎实的基准分析**：对语料库结构和问题类型分布做了细致剖析，直接指导方法设计。

### 8. 不足与局限
- **实验验证不足**：缺乏消融研究，无法证明各组件（如步数门控、时间戳、自适应预算）的必要性和独立效果。
- **强依赖闭源模型**：骨干模型 Gemini 3.1 Pro 为闭源，其内部行为不详，难以复现和公平对比。
- **规则分类器泛化性**：问题类型分类器基于模板匹配，可能无法应对 TimeLogic 基准之外的、具有不同措辞或更复杂逻辑的问题。
- **预算参数手动设定**：采样参数、时长阈值等由人工根据先验知识设定，缺乏自动化或自适应学习机制。
- **无统计与错误分析**：未报告各类别的准确率，难以判断方法是否在某些类别（如 “Immediately Next”）仍然存在明显短板。

（完）
