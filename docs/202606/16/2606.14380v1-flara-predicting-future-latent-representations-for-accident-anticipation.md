---
title: "FLaRA: Predicting Future Latent Representations for Accident Anticipation"
title_zh: FLaRA：预测未来潜在表征进行事故预测
authors: "Lorenzo Caselli, Tomaso Trinci, Tommaso Bianconcini, Simone Magistri, Leonardo Taccari, Francesco Sambo, Andrew D. Bagdanov"
date: 2026-06-12
pdf: "https://arxiv.org/pdf/2606.14380v1"
tags: ["query:vtg"]
score: 8.0
evidence: 从行车视频中预测未来潜在表征以检测事故，属于在线事件检测
tldr: 现有基于行车记录仪的事故预测方法通常直接映射视觉上下文到碰撞概率，忽略未来场景的动态演变。本文提出 FLaRA 模型，基于 V-JEPA2 架构预测未来潜在表征，并联合优化特征重建与分类损失，从而利用预测的未来信息进行事故判别。在 Nexar 及多个跨域数据集上，FLaRA 取得领先性能，并实现可靠的早期预警，为智能交通系统的事故预测提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.14380v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1383, \"height\": 756, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14380v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 546, \"height\": 422, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.14380v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 898, \"height\": 210, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14380v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 693, \"height\": 244, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14380v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 589, \"height\": 514, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14380v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 827, \"height\": 506, \"label\": \"Table\"}]"
motivation: 现有方法未显式建模驾驶场景的未来演变，限制了对事故前兆的捕捉能力。
method: 提出 FLaRA，条件于观测帧通过预测器生成未来特征表示，并用联合损失（特征重建和分类）训练。
result: 在 Nexar、DAD、DADA-2000 和 DoTA 上均达到最优，并展现真实的早期预警能力。
conclusion: 通过预测未来表征突破直接映射的局限，有效提升事故预测性能与预警实用性。
---

## 摘要
通过行车记录仪视频预测交通事故是智能交通系统中的一项关键挑战。现有方法通常直接将视觉上下文映射到碰撞概率，而没有显式地对驾驶场景的未来演变进行建模。在本文中，我们提出了FLaRA（通过预测未来潜在表征进行事故预测），这是一种新颖的预测架构，它通过预测未来潜在表征来进行事故预测，从而改变了这一范式。基于视频联合嵌入预测架构（V-JEPA2），我们的模型在观察到的上下文帧上调节预测器网络，以预测场景即将出现的潜在特征。然后，分类器在这些预测的未来表征上操作，而不是仅基于过去的观察。为了确保这些预测能够基于现实的未来动态，我们引入了一个联合训练目标，同时优化辅助特征级重构损失和交叉熵分类损失。在Nexar数据集上的广泛评估，以及在DAD、DADA-2000和DoTA基准上的跨领域验证，表明我们的方法在保持现实预警能力的同时，达到了最先进的性能。

## Abstract
Anticipating traffic accidents from dashcam videos is a critical challenge in intelligent transportation systems. Existing methods typically map visual context directly to a collision probability without explicitly modeling the future evolution of the driving scene. In this paper we propose FLaRA (Predicting Future Latent Representations for Accident Anticipation), a novel predictive architecture that shifts this paradigm by forecasting future latent representations for accident anticipation. Building upon the Video Joint-Embedding Predictive Architecture (V-JEPA2), our model conditions a predictor network on observed context frames to predict the forthcoming latent features of the scene. A classifier then operates on these predicted future representations rather than only on past observations. To ensure these forecasts remain grounded in realistic future dynamics, we introduce a joint training objective that simultaneously optimizes an auxiliary feature-level reconstruction loss and a cross-entropy classification loss. Extensive evaluations on the Nexar dataset, alongside cross-domain validations on the DAD, DADA-2000, and DoTA benchmarks, demonstrate that our approach achieves state-of-the-art performance while maintaining realistic early warning capabilities.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **研究背景**：行车记录仪视频广泛普及，基于视觉的事故预测（Accident Anticipation）对提升交通安全具有重要价值。现有方法大多直接将观测到的历史帧特征映射为碰撞概率，缺乏对驾驶场景**未来动态演变的显式建模**。
- **核心挑战**：如何让模型不仅依赖当前可见的场景信息，还能“预见”未来时刻的潜在状态，从而在事故发生前更早、更准确地发出预警。
- **研究动机**：受视频预测模型（如V‑JEPA2）及动作预测领域的启发，提出一种“先预测未来潜在表征，再基于未来表征进行分类”的新范式，以弥补现有方法对时序前向推理能力的不足。

### 2. 论文提出的方法论

- **整体架构 (FLaRA)**：基于V‑JEPA2视频基础模型，包含三个主要组件：
  - **视频编码器 (fθ)**：提取视觉特征。
  - **未来预测器 (gϕ)**：根据当前上下文帧的特征预测未来帧的潜在表征。
  - **注意力探针与分类头 (pψ)**：对预测的未来表征进行事故分类。
- **训练阶段 (见图1a)**：
  - 输入分为**上下文窗口** (xc) 和**未来窗口** (xf)，上下文窗口不包含事故瞬间。
  - 编码器同时处理 `[xc, xf]` 得到目标未来表征 `˜zf`，同时仅编码 `xc` 得到 `zc`。
  - 预测器以 `zc` 为输入，输出预测的未来表征 `ˆzf`。
  - **联合训练目标**：
    - **重构损失 L_rec**：使用 Smooth‑L1 计算 `ˆzf` 与停止梯度的目标 `˜zf` 之间的差异，确保预测的未来特征符合真实未来动态。
    - **分类损失 L_cls**：将 `ˆzf` 送入注意力探针得到事故概率，与真实标签计算交叉熵。
    - 总损失：`L = L_cls + λ L_rec`，λ 为超参数（本文取10）。
- **推理阶段 (见图1b)**：仅使用上下文窗口 `xc`，预测器输出 `ˆzf`，再经过探针和分类头得到预测结果，完全无需访问真实未来帧。
- **与 V-JEPA2 的区别**：原始 V‑JEPA2 通过自监督学习重建随机掩码的时空块；FLaRA 将重建目标改为预测后续时序帧的潜在表征，并将该重构损失作为辅助任务与分类任务联合优化。

### 3. 实验设计

- **训练与评估数据集**：
  - **主训练集**：Nexar 数据集训练分割（1500 样本，正负平衡），每个正样本带有警报时间 `t_alert` 和事件时间 `t_event`。
  - **主测试集**：Nexar 测试分割（1344 样本，672正/672负），正样本按三种时间偏移（0.5s, 1.0s, 1.5s）裁剪进行评估。
  - **跨域泛化测试集**：DAD、DADA‑2000、DoTA 三个公开基准，使用由前人重标注的、仅含自车碰撞的子集，并采用相同的方式构造负样本。
- **对比方法**：
  - UString (2020)、DSTA (2021)：传统的逐帧递归/Transformer方法，使用CCD数据集预训练权重。
  - BADAS (2025)：同样基于V‑JEPA2但直接对观测上下文特征进行分类，是目前的最先进方法。
- **评价指标**：AP (Average Precision)、AUC (Area Under ROC Curve)、mTTA (mean Time‑to‑Accident)。推理时采用滑动窗口方式，并保证窗口结束时间早于事件时间。

### 4. 资源与算力

- **硬件**：所有实验均在**单张 NVIDIA RTX 4090 GPU** 上完成。
- **训练配置**：
  - 输入：上下文和未来各16帧，8 fps，分辨率 256×256。
  - 批量大小：4。
  - 优化器：AdamW，权重衰减 1×10⁻²，峰值学习率 2.5×10⁻⁵，余弦退火调度。
  - 训练轮数：30 epochs。
  - 可训练参数：84M（冻结除编码器最后一块外的部分）。文中未明确提及单次训练总时长，但提供了完整的硬件与超参数配置。

### 5. 实验数量与充分性

- **实验组数**：
  - **主对比实验**：在 Nexar 测试集上与 DSTA、UString、BADAS 比较（表II）。
  - **跨数据集泛化实验**：DAD、DADA‑2000、DoTA 三个域外数据集上的性能对比（表III）。
  - **消融实验**（表IV）：包括冻结骨干、去除重构损失、仅用编码器特征、分类时拼接上下文特征等 5 个变体，并同时展示了所有四个数据集的性能。
  - **预测时间窗口分析**：在 Nexar 上按不同时间偏移分组比较 AUC（图2）。
- **公平性与客观性**：
  - 所有对比方法均使用公开检查点或按原文设定复现，评估协议统一。
  - 消融实验对照清晰，逐步验证了微调预测器、重构损失、仅用未来特征分类等各模块的作用。
  - 结果报告三次运行的均值，减少随机性影响。
- **充分性评价**：实验涵盖了领域内主流 benchmark、多种损失组合与特征利用方式，并通过跨数据集验证了泛化能力，整体实验设计较为充分。

### 6. 论文的主要结论与发现

- FLaRA 在 Nexar 数据集上取得了 **AP 86.6，AUC 87.3** 的最优成绩，超过 BADAS，且可训练参数仅为后者的约 1/4 (84M vs 332M)。
- 在 mTTA 指标上，FLaRA 达到 **3.744 秒**，与 BADAS 相当，证明性能提升并未牺牲预警的及时性。
- 跨数据集泛化实验中，FLaRA 在**类别分布与现实更接近的 DAD 数据集**上获得大幅提升（AP +18.4），表明预测未来表征的方式对稀有事件学习更有效。
- 消融实验证实：
  - 联合重构损失 L_rec 对于引导预测器学习有意义的未来动态至关重要，在 DAD 上 AP 从 25.6 提升至 67.3。
  - 仅使用预测的未来特征进行分类效果最佳；加入当前上下文特征反而可能引入冗余信息，降低性能。
- 时间偏移分析显示，随着预测时间提前（0.5s → 1.5s），FLaRA 相较 BADAS 的优势逐渐扩大，说明在需要更长前瞻时间、而当前视觉线索稀少时，预测未来表征的方法更具优势。

### 7. 优点

- **创新范式**：将“预测未来潜在表征再进行分类”的思路首次引入事故预测领域，改变了过去直接对历史特征分类的惯例。
- **多任务协同训练**：通过重构未来特征与分类损失联合优化，既保持了预测的物理合理性，又提升了罕见事故的识别能力。
- **参数高效**：仅微调少量参数（84M）即超越参数量大得多（332M）的前沿方法，显存和部署成本更低。
- **泛化能力强**：在无任何目标域微调的情况下，在多个差异性较大的基准上取得有竞争力的结果，尤其在长尾分布的真实场景下优势明显。
- **实验设计严谨**：消融实验对比细致，覆盖训练策略、损失函数、特征源等重要因素；评估协议统一，考虑了滑动窗口和多时间偏移，较为客观。

### 8. 不足与局限

- **预警时间提升有限**：虽然预测精度提高，但 mTTA 与最强基线 BADAS 基本持平，未能显著延长有效预警时间，这可能受限于模型固定的 2 秒观测窗口和预测范围。
- **固定时间预测范围**：预测器采用固定的未来时长（16 帧，约 2 秒），缺乏对变化的时间距离的自适应能力，离事件较远时可能无法进一步提前预测。
- **计算资源描述不完整**：虽然报告了 GPU 型号和训练轮数，但未提供单次训练的总耗时或吞吐量，难以直接评估部署效率。
- **数据集偏差风险**：尽管测试了多个数据集，但所有数据均来自车辆前向视角，场景多样性（天气、地理、道路类型）可能存在局限；此外，DAD 等子集的正样本极少（13 个），结论的统计稳定性可能受影响。
- **负样本构造方式**：对 DADA‑2000 和 DoTA，从正样本视频开头截取 4 秒片段作为负样本，虽保证了无事件重叠，但可能引入简单负例，使性能估计偏乐观。
- **应用限制**：模型需要 2 秒的视频历史才能开始推理，对于极短视频或需要超实时响应的场景，可能造成预警延迟；实际部署时仍需权衡精度与推理延迟。

（完）
