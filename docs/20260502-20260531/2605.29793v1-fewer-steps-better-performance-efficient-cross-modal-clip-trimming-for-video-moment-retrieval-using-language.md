---
title: "Fewer Steps, Better Performance: Efficient Cross-Modal Clip Trimming for Video Moment Retrieval Using Language"
title_zh: 更少步骤，更优性能：面向语言视频时刻检索的高效跨模态片段裁剪
authors: "Xiang Fang, Daizong Liu, Wanlong Fang, Pan Zhou, Zichuan Xu, Wenzheng Xu, Junyang Chen, Renfu Li"
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.29793v1"
tags: ["query:vtg"]
score: 10.0
evidence: 基于跨模态片段修剪的高效视频时刻检索，用于语言查询
tldr: 视频时刻检索需从长视频定位查询相关片段，现有方法稀疏下采样固定片段，易滤除关键帧，导致边界模糊和跨模态错位，引发双重偏差。本文提出SpotVMR，设计片段搜索模型动态定位有前景区域，并引入语义索引特征捕获上下文，高效修剪相关片段，通过蒸馏损失联合训练。实验表明，SpotVMR在三个数据集上作为即插即用模块，大幅提升最新方法效率且保持检索准确性。为长视频理解提供了轻量高效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.29793v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 840, \"height\": 716, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29793v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1826, \"height\": 1055, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29793v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 851, \"height\": 327, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29793v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 841, \"height\": 844, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.29793v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 829, \"height\": 1548, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29793v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 861, \"height\": 351, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29793v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1727, \"height\": 281, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29793v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 839, \"height\": 276, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29793v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 849, \"height\": 207, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29793v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1797, \"height\": 283, \"label\": \"Table\"}]"
motivation: 现有VMR方法对长视频稀疏下采样固定片段，易过滤查询相关帧，导致边界模糊与跨模态未对齐，引入推理偏差。
method: 提出SpotVMR，通过片段搜索模型学习查询条件定位，利用语义索引特征低成本修剪相关片段，结合蒸馏损失优化端到端训练。
result: 在三个数据集上，SpotVMR作为即插即用模块显著提升现有SOTA方法的效率，同时保持高检索性能。
conclusion: SpotVMR缓解了长视频检索的效率和精度权衡，可作为插件广泛部署，为高效视频理解提供了实用方案。
---

## 摘要
给定未修剪视频和句子查询，基于语言的视频时刻检索（VMR）旨在定位与查询相关的目标时刻。由于未修剪视频过长，几乎所有现有VMR方法首先对每个未修剪视频稀疏下采样为多个固定长度的视频片段，然后进行多模态交互，涉及查询特征和昂贵的片段特征以进行推理，这对于长达数小时的真实世界长视频来说是不可行的。由于视频下采样为固定长度片段，一些与查询相关的帧可能被过滤掉，这将模糊目标时刻的具体边界，将相邻的无关帧作为新边界，容易导致跨模态不对齐，并引入边界偏差和推理偏差。为此，本文提出一种高效方法SpotVMR，用于裁剪查询相关片段。此外，我们提出的SpotVMR可作为即插即用模块，在保持良好检索性能的同时，为最先进的VMR方法实现效率提升。具体地，我们首先设计了一种新颖的片段搜索模型，该模型学习根据语言查询来识别有潜力的视频区域进行搜索。然后，我们引入一组低成本的语义索引特征，以捕获对象和交互的上下文，这些上下文指示在何处搜索与查询相关的时刻。此外，利用蒸馏损失来解决片段选择器和VMR模型端到端联合训练中出现的优化问题。在三个具有挑战性的数据集上的大量实验证明了其有效性。

## Abstract
Given an untrimmed video and a sentence query, video moment retrieval using language (VMR) aims to locate a target query-relevant moment. Since the untrimmed video is overlong, almost all existing VMR methods first sparsely down-sample each untrimmed video into multiple fixed-length video clips and then conduct multi-modal interactions with the query feature and expensive clip features for reasoning, which is infeasible for long real-world videos that span hours. Since the video is downsampled into fixed-length clips, some query-related frames may be filtered out, which will blur the specific boundary of the target moment, take the adjacent irrelevant frames as new boundaries, easily leading to cross-modal misalignment and introducing both boundary-bias and reasoning-bias. To this end, in this paper, we propose an efficient approach, SpotVMR, to trim the query-relevant clip. Besides, our proposed SpotVMR can serve as plug-and-play module, which achieves efficiency for state-of-the-art VMR methods while maintaining good retrieval performance. Especially, we first design a novel clip search model that learns to identify promising video regions to search conditioned on the language query. Then, we introduce a set of low-cost semantic indexing features to capture the context of objects and interactions that suggest where to search the query-relevant moment. Also, the distillation loss is utilized to address the optimization issues arising from end-to-end joint training of the clip selector and VMR model. Extensive experiments on three challenging datasets demonstrate its effectiveness.

---

## 论文详细总结（自动生成）

好的，这是对你提供的论文的结构化、深入、客观的总结。

### **1. 论文核心问题与研究背景**

*   **核心问题**：论文聚焦于视频时刻检索任务中的效率与精度权衡问题。现有主流方法在处理长视频时，会将其稀疏降采样为固定长度的片段，这一做法不仅计算开销巨大，还会因为过滤掉查询相关帧而导致**边界模糊**和**跨模态不对齐**，最终引入**边界偏差**与**推理偏差**。

*   **研究背景与动机**：
    *   **任务挑战**：VMR任务需要从长达数小时的视频中，根据一句自然语言查询，定位一个极短的、语义相关的目标片段。这要求模型不仅要理解复杂的跨模态语义，还要能在海量无关内容中精准定位。
    *   **现有方法的局限性**：
        1.  **计算不可行**：对长视频所有帧进行昂贵的视觉特征提取和多模态交互推理，算力开销巨大，难以在实际应用中部署。
        2.  **固定采样引入偏差**：固定的稀疏降采样策略不可避免地会丢失关键帧信息，损害模型定位的精确性。
    *   **核心直觉**：视频中绝大部分内容与查询无关。利用高层次的视觉语义（如背景、外观、动作）作为“索引”，可以快速、廉价地预览整个视频，从而智能地筛选出一小部分可能与查询相关的片段进行后续的精细处理。

### **2. 提出的方法论**

论文的核心思想是设计一个名为 **SpotVMR** 的高效即插即用模块。它通过快速、低成本的“预览-筛选”机制，动态地选择少量查询相关片段，从而大幅减少后续昂贵计算的开销。

*   **核心思想与架构**：SpotVMR 作为预处理模块，集成到现有VMR模型之前。它首先用低成本的语义索引器扫描全视频，然后通过一个名为 **RetrievalSpotter** 的网络迭代式地筛选出查询相关的一个小子集片段（Clip）。最终，只有这些被选中的片段才被送入后续的VMR模型进行精细处理。

*   **关键技术细节**：
    1.  **低成本语义索引特征 (BAM Features)**：
        *   为高效预览，论文设计了三类可低成本提取的语义索引特征：
            *   **背景特征 (Background)**：使用预训练的EfficientNet-b0，捕捉场景信息。
            *   **外观特征 (Appearance)**：使用自监督的VICReg网络，捕捉物体属性并缩小同类视觉差异。
            *   **运动特征 (Motion)**：使用预训练的C3D网络，通过片段的首末帧提取运动信息。
        *   这些BAM特征仅从每个片段中采样一帧图像进行计算，成本极低。

    2.  **片段搜索与选择模块 (RetrievalSpotter)**：
        *   **迭代式选择**：该模块包含一个跨模态交互层和一个片段选择策略。它在 `B` 个递归步骤内，交替进行“选片段→提取昂贵特征→用昂贵特征再预览”的过程。
        *   **流程**：
            *   **输入**：BAM语义索引 `s`，查询特征 `q`，以及上一步选中的片段昂贵特征 `v'`。
            *   **交叉融合**：将 `s` 与 `v'` 融合，再通过一个跨模态融合模块与 `q` 进行交互，得到指导特征 `c'`。
            *   **片段选择**：一个两层的MLP根据 `c'` 为每个片段输出一个二进制选择值 `p`，决定该片段是否被选中。
            *   **迭代**：被选中的片段的昂贵C3D特征被计算出来，并替代之前的 `v'`，进入下一个迭代步，实现预览的逐步精细化。最终输出一个累计的被选片段子集。

    3.  **优化与训练策略**：
        *   **片段选择损失 ($L_{sel}$)**：通过预设超参数 `γ` 来控制被选片段占总片段的期望比例，防止过度选择或选择不足。
        *   **VMR任务损失 ($L_{vmr}$)**：包含边界预测损失 ($L_{boundary}$) 和查询感知视觉增强损失 ($L_{qav}$) ，用于监督最终的片段定位。
        *   **特征蒸馏损失 ($L_{ftd}$)**：采用两阶段训练，先训练一个不看片段选择、处理全视频的“教师模型”，然后让融合了SpotVMR的“学生模型”在跨模态特征层面模仿教师模型的特征。这有效解决了片段选择模块与VMR主干网络端到端联合训练的优化难题。
        *   **不可微采样处理**：对于片段选择产生的二进制输出，使用Gumbel-Softmax重参数化技巧使其可微分。

### **3. 实验设计**

*   **使用数据集/场景**：实验在VMR领域三个主流且具有挑战性的基准数据集上进行：
    *   **ActivityNet Captions**：包含2万个来自YouTube的未修剪视频及10万条描述。
    *   **TACoS**：包含127个烹饪场景的未修剪视频。
    *   **Charades-STA**：包含约6672个日常室内活动视频。

*   **评价指标**：采用通用的召回率指标“R@n, IoU=m”，即在前n个预测结果中，与真实值的交并比大于m的查询所占百分比。具体参数设定遵循领域惯例。

*   **对比方法**：与广泛的SOTA方法进行了对比，主要分为两类：
    *   **Top-down方法**：CTRL, SCDM, CMIN, 2D-TAN, DRN, MMN。
    *   **Bottom-up方法**：GDP, LGI, VSLNet, IVG-DCL, ACRM。

### **4. 资源与算力**

*   论文中未明确提及训练所使用的GPU型号、数量或具体的训练总时长。这是实验细节描述的一个缺失项。

### **5. 实验数量与充分性**

论文设计并执行了数量较为可观的实验，以支持其核心论点，实验整体上**较为充分、客观且公平**。

*   **主对比实验**：在三个数据集上，与十余种SOTA方法进行了全面的性能比较（参见Table 1），结果显示本方法均大幅领先。
*   **效率对比实验**：专门设计实验对比了模型处理100段视频的总耗时（参见Table 2），直观展示了其在特征提取和推理效率上的显著优势。
*   **消融实验**：
    *   **模块消融**：逐一移除关键损失函数（$L_{qav}$, $L_{sel}$, $L_{ftd}$），验证了每个组件对最终性能的贡献（参见Table 3）。
    *   **即插即用特性验证**：将SpotVMR作为插件应用于2D-TAN和MMN两种不同架构的SOTA模型上，结果均显示性能提升，有力证明了其通用性和有效性（参见Table 4）。
    *   **BAM特征消融**：分别或组合使用背景(B)、外观(A)、运动(M)三类语义索引特征，验证了每类特征的重要性，且三者结合效果最佳（参见Table 5）。
    *   **关键超参数分析**：分析了递归步数`B_max`（参见Table 6）和损失函数权重`α, β, γ`（参见Figure 3）对性能的影响。

### **6. 主要结论与发现**

*   **高效性**：SpotVMR通过“低成本语义索引预览”和“迭代式筛选”，能显著减少需要昂贵特征提取和多模态交互的视频片段数量，极大提升了VMR任务的运行效率。
*   **有效性**：该方法不仅没有损害性能，反而通过聚焦查询相关片段、剔除无关噪声，超越了所有对比的全量处理模型，实现了SOTA的检索精度。
*   **通用即插即用性**：SpotVMR可以作为一个通用插件，无缝集成到现有的不同架构（Top-down/Bottom-up）的VMR模型中，在不改变原模型结构的前提下实现效率与性能的双重提升。

### **7. 优点**

*   **高度创新：** 颠覆了传统VMR“全量处理”或“固定采样”的思路，提出了“动态、查询感知的片段筛选”这一新范式。
*   **设计精巧：**
    *   **BAM特征设计**巧妙地将定位所需的高层语义（背景、外观、动作）解耦，并以低成本方式获取。
    *   **迭代式检索选择器（RetrievalSpotter）** 的递归“预览-选择-再预览”机制，使得搜索过程由粗到细，逐步聚焦。
*   **实用性强：** 该方法是一个即插即用的轻量级模块，无需改变下游模型结构，极易部署到现有系统中，对于推动VMR从学术研究走向长视频落地应用具有重要意义。
*   **优化策略扎实：** 引入蒸馏损失解决了片段选择这种离散决策过程与检索任务联合训练的优化难题，保障了训练的稳定性和最终性能。
*   **实验完整：** 从准确性、效率、模块贡献、通用性等多个角度进行了论证，说服力强。

### **8. 不足与局限**

*   **对语义索引器的依赖**：BAM索引特征的质量依赖于预训练的分类/自监督模型，其领域适应性和泛化能力未在论文中深入探讨。对于背景、外观、动作分类不明显的抽象或高度专业领域的长视频，性能可能会下降。
*   **计算是“节省”而非“消除”**：该方法并未完全消除全视频的扫描，所有片段仍需提取廉价的BAM索引特征。对于极长的、或要求极致延迟的应用，前期的扫描开销可能仍然是一个瓶颈。
*   **资源与算力报告不完整**：论文未提供任何关于GPU型号、训练时长、显存占用等关键算力消耗数据，这不利于其他研究者复现和评估其实际部署成本。
*   **超参数敏感性**：Gamma等超参数直接控制了选择片段的比例，需要在训练精度和效率之间权衡。在实际部署中，可能需要针对不同数据分布进行调优。
*   **两阶段训练的复杂度**：引入教师模型和蒸馏损失虽然解决了优化问题，但也增加了训练流程的复杂度和时间成本。

（完）
