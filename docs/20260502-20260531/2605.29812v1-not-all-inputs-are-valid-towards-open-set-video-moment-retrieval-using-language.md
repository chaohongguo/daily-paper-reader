---
title: "Not All Inputs Are Valid: Towards Open-Set Video Moment Retrieval Using Language"
title_zh: 并非所有输入都有效：迈向基于语言的开集视频时刻检索
authors: "Xiang Fang, Wanlong Fang, Daizong Liu, Xiaoye Qu, Jianfeng Dong, Pan Zhou, Renfu Li, Zichuan Xu, Lixing Chen, Panpan Zheng, Yu Cheng"
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.29812v1"
tags: ["query:vtg"]
score: 10.0
evidence: 探索开放集视频时刻检索以处理分布外查询
tldr: 现有视频时刻检索假设所有查询与视频相关，易因无关查询导致错误。本文提出开放集视频时刻检索任务，需同时拒绝无关查询并检索相关时刻。OpenVMR模型利用归一化流建模查询分布，通过不确定性分数分离相关与无关查询，再执行粗、细粒度匹配与正无标签学习。在三个数据集上验证了模型有效性，为高风险场景提供可靠方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.29812v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 887, \"height\": 522, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29812v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1841, \"height\": 852, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29812v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 863, \"height\": 544, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29812v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 470, \"height\": 295, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29812v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 432, \"height\": 291, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.29812v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 911, \"height\": 205, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29812v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 888, \"height\": 1741, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29812v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 858, \"height\": 313, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29812v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 873, \"height\": 530, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29812v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 908, \"height\": 241, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29812v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 865, \"height\": 530, \"label\": \"Table\"}]"
motivation: 现有VMR模型默认所有查询有效，在开放场景下无关查询可能导致严重误判，急需统一处理查询判别与检索的新方法。
method: 基于归一化流构建查询高斯分布，用不确定性分数与特征聚合确定相关边界，结合视频-查询匹配、帧-查询匹配及正无标签检索。
result: 在三个标准数据集上，OpenVMR有效区分并拒绝无关查询，同时准确检索相关时刻，性能超越闭集基线。
conclusion: 首次定义OS-VMR任务，提出OpenVMR模型，通过查询判别与检索协同，解决了开放场景下的可靠性问题。
---

## 摘要
视频时刻检索（VMR）旨在从未经修剪的视频中检索与句子查询相对应的特定时刻。尽管近期工作在该任务上取得了显著进展，但它们都隐含地基于闭集假设，即所有给定查询都与视频相关。在开集场景下，面对分布外（OOD）查询，它们仍会进行错误检索，这在高风险场景（如犯罪活动检测）中可能导致不可挽回的损失。为此，我们开创性地探索了一种全新的VMR设置，称为开集视频时刻检索（OS-VMR），其中我们不仅需要基于分布内（ID）查询检索精确时刻，还需拒绝OOD查询。本文首次尝试迈向OS-VMR，并提出了一种新模型OpenVMR，该模型首先基于归一化流技术区分ID和OOD查询，然后基于ID查询进行时刻检索。具体而言，我们首先通过构建归一化流来学习ID分布，并假设ID查询分布服从多元高斯分布。随后，引入不确定度得分来搜索ID-OOD分离边界。之后，通过拉近ID查询特征来细化ID-OOD边界。另外，分别设计了视频-查询匹配和帧-查询匹配，以进行粗粒度和细粒度的跨模态交互。最后，引入正无标签学习模块进行时刻检索。在三个VMR数据集上的实验结果表明了我们的OpenVMR的有效性。

## Abstract
Video Moment Retrieval (VMR) targets to retrieve the specific moment corresponding to a sentence query from an untrimmed video. Although recent works have made remarkable progress in this task, they implicitly are rooted in the closed-set assumption that all the given queries as video-relevant\footnote{In this paper, we treat ``video-relevant query'' as ``in-distribution (ID) query'' and ``video-irrelevant query'' as ``out-of-distribution (OOD) query''.}. Given an OOD query in open-set scenarios, they still utilize it for wrong retrieval, which might lead to irrecoverable losses in high-risk scenarios, \textit{e.g.}, criminal activity detection. To this end, we creatively explore a brand-new VMR setting termed Open-Set Video Moment Retrieval (OS-VMR), where we should not only retrieve the precise moments based on ID query, but also reject OOD queries. In this paper, we make the first attempt to step toward OS-VMR and propose a novel model \textbf{OpenVMR}, which first distinguishes ID and OOD queries based on the normalizing flow technology, and then conducts moment retrieval based on ID queries. Specifically, we first learn the ID distribution by constructing a normalizing flow, and assume the ID query distribution obeys the multi-variate Gaussian distribution. Then, we introduce an uncertainty score to search the ID-OOD separating boundary. After that, we refine the ID-OOD boundary by pulling together ID query features. Besides, video-query matching and frame-query matching are designed for coarse-grained and fine-grained cross-modal interaction, respectively. Finally, a positive-unlabeled learning module is introduced for moment retrieval. Experimental results on three VMR datasets show the effectiveness of our OpenVMR.

---

## 论文详细总结（自动生成）

好的，以下是基于提供的论文内容生成的结构化中文总结。

### 1. 论文的核心问题与整体含义

本论文指出现有的视频时刻检索任务存在一个关键盲点：**所有方法都基于一个闭集假设，即认为用户输入的所有查询文本都与当前视频内容相关**。

-   **研究动机与问题：** 在真实世界的开放场景中，系统经常会收到与视频内容无关的查询（如，在一个厨房烹饪视频中搜索“洗车的动作”）。现有VMR模型会“强行”为这些无关查询检索出一个错误时刻，这在犯罪活动检测等高风险场景中可能导致灾难性后果。因此，一个实用的系统不仅需要为相关查询定位时刻，还必须具备**识别并拒绝无关查询**的能力。
-   **核心任务定义：** 为此，论文首次提出了一个名为**开集视频时刻检索**的全新任务。其核心在于：给定一个未剪辑视频和一个查询，系统需完成双重任务：
    1.  **分布外查询拒绝：** 若查询与视频无关（OOD查询），则拒绝该请求。
    2.  **分布内查询检索：** 若查询与视频相关（ID查询），则精确定位目标时刻。

### 2. 论文提出的方法论

论文提出了`OpenVMR`模型，通过四个核心模块来解决OS-VMR任务，其整体流程如下：

-   **核心思想：** 首先，模型需要学习“视频相关查询”所特有的特征分布（ID分布）。然后，基于此分布建立一个决策边界，用以区分ID和OOD查询。最后，针对识别出的ID查询执行精细的时刻检索。

-   **关键技术细节：**
    1.  **ID知识获取：** 使用**归一化流技术**来学习ID查询的特征分布。该模块将查询特征映射到一个服从**多元高斯分布**的潜在空间，并通过最大化ID查询在此空间中的似然概率来训练网络，从而准确捕捉ID查询的分布特征。
    2.  **不确定性感知的OOD边界推理：** 利用训练好的归一化流，为每个查询计算一个对数似然值。将其转换为**不确定度得分**后，通过设定一个超参数（例如，取ID查询对数似然分布的第5个百分位数）来作为ID的内部边界，并引入一个间隔超参数来定义OOD的外部边界。查询的不确定度得分若落在ID边界内则为ID查询，否则为OOD查询。
    3.  **ID-OOD边界优化：** 设计了一个**三元组损失函数**来进一步拉开ID和OOD查询在特征空间中的距离。它“拉近”属于ID的查询特征，同时“推开”那些落在边界附近甚至内部的OOD查询特征，从而形成一个更清晰、更鲁棒的分离边界。
    4.  **跨模态交互与检索：** 对于判别为ID的查询，模型进行精细的时刻检索，包含三个步骤：
        -   **视频-查询匹配：** 使用注意力池化和**余弦相似度**进行粗粒度的全局语义对齐。
        -   **帧-查询匹配：** 计算每个视频帧与查询的匹配概率，用于细粒度的局部对齐并增强相关帧的特征。
        -   **正无标签学习：** 将帧-查询匹配得分高的预定义提议作为“正样本”，得分低的作为“无标签背景样本”，并从中选出可靠的低分样本作为“负样本”。最终使用**二元交叉熵损失**来区分前景和背景时刻，并进行边界回归。

### 3. 实验设计

-   **数据集与场景：**
    -   在三个标准VMR数据集上进行了实验：**ActivityNet Captions**、**Charades-STA** 和 **TACoS**，覆盖了开放域活动、室内场景和烹饪场景。
    -   实验设定分为两种：
        -   **闭集设定：** 训练和测试均使用同一数据集的查询和视频。
        -   **开集设定：** 在一个数据集上训练模型，但在测试时混入所有三个数据集的查询，以模拟真实的OOD场景。

-   **基准方法与对比对象：**
    -   将`OpenVMR`与两大类最先进方法进行了比较：
        1.  **全监督方法：** CTRL、2D-TAN、DRN、RaNet、MIGCN、MMN、G2L、MomentDiff等。
        2.  **弱监督方法：** ICVC、LCNet、VCA、WSTAN、CNM等。
    -   评估指标：对于ID查询，使用“R@n, IoU=m”评估时刻检索准确性；对于开集性能，使用**AUROC** 和 **AUPR**评估OOD查询检测能力。

### 4. 资源与算力

-   **硬件：** 论文明确指出使用**单张Nvidia TITAN XP GPU**进行实验。
-   **训练时长：** 模型训练了**200个epoch**，并采用了早停策略。
-   **推理效率：** 在TACoS数据集上，`OpenVMR`的推理时间仅为**0.08秒**，模型大小为**92MB**，相比其他对比方法（如MomentDiff的1.85秒/248MB）显著更快、更轻量。

### 5. 实验数量与充分性

论文进行了**充足且多维度的实验**来验证模型的有效性，确保了客观与公平。

-   **主要对比实验：** 在3个数据集上，分别在闭集和开集两种设定下与大量基线模型进行了性能对比。
-   **消融研究：** 系统地移除了`OpenVMR`的三大核心模块（ID知识获取、OOD边界推理、边界优化）以及跨模态交互的三个组件（视频-查询匹配、帧-查询匹配、正无标签学习），共进行了**超过6组消融实验**，以验证各组件的贡献。
-   **参数敏感性与超参分析：** 探讨了耦合层层数、潜在变量分布类型、百分位数、间隔超参数以及损失函数权重等多个关键参数的影响。
-   **可视化分析：** 展示了闭集和开集场景下的预测结果对比，以及训练过程的收敛曲线。

### 6. 论文的主要结论与发现

-   `OpenVMR`在所有数据集上均取得了**当前最先进的性能**，在闭集和开集设定下均显著优于现有方法。
-   在开集设定下，传统VMR模型性能急剧下降，因为它们无法区分和拒绝OOD查询。而`OpenVMR`通过引入OOD查询检测机制，实现了可靠的时刻检索。
-   消融实验证明，模型中的ID分布学习、OOD边界推理及边界优化三大模块均对最终性能有显著贡献。
-   该模型在保持高精度的同时，还具备**更高的计算效率**（更短的推理时间和更小的模型体积）。

### 7. 优点

-   **问题创新性：** 首次定义了“开集视频时刻检索”任务，将OOD检测与VMR相结合，更贴近真实世界应用需求。
-   **方法新颖有效：** 创新性地利用归一化流来建模查询分布，并结合不确定度得分和边界优化策略，优雅地解决了ID/OOD查询的判别问题。
-   **模块化设计：** 模型结构清晰，各模块职责明确，易于理解和改进。
-   **实验充分扎实：** 在多个数据集和设定下进行了全面评估，消融研究透彻，且兼顾了性能与效率的对比。

### 8. 不足与局限

-   **OOD数据选择：** 实验中的OOD查询来源于**其他数据集的查询**，虽然能模拟分布外情况，但与真实世界中更具攻击性或语义混淆的OOD查询可能存在差异。
-   **OOD边界依赖：** OOD边界的确定依赖于手动设定的超参数（如百分位数和间隔），这可能需要对特定数据集进行调优，自适应能力有待提升。
-   **分布假设：** 模型假设ID查询在潜在空间服从高斯分布，对于某些真实分布与该假设差异较大的复杂场景，其效果可能会受影响。
-   **探索范围有限：** 该任务作为首次尝试，论文未深入探讨更复杂的开集情形，如视频本身也是OOD样本，或者同时存在多个ID和OOD查询的混合场景。

（完）
