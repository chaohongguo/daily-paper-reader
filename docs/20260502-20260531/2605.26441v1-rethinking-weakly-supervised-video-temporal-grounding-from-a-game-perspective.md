---
title: Rethinking Weakly-supervised Video Temporal Grounding From a Game Perspective
title_zh: 从博弈视角重新思考弱监督视频时序定位
authors: "Xiang Fang, Zeyu Xiong, Wanlong Fang, Xiaoye Qu, Chen Chen, Jianfeng Dong, Keke Tang, Pan Zhou, Yu Cheng, Daizong Liu"
date: 2026-05-26
pdf: "https://arxiv.org/pdf/2605.26441v1"
tags: ["query:vtg"]
score: 9.0
evidence: 解决视频时序定位任务
tldr: 弱监督视频时序定位任务现有方法依赖粗粒度对齐和复杂提案，忽略细粒度帧词匹配。本文首次引入博弈视角，将视频帧与查询词视为合作博弈玩家，基于多元合作博弈论建模多粒度跨模态交互，学习帧词贡献得分。无需预生成提案，直接利用查询引导的帧级得分实现时刻精确定位。在Charades-STA和ActivityNet Caption数据集上显著超越现有方法，为弱监督视频定位提供博弈论新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.26441v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1265, \"height\": 296, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.26441v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1270, \"height\": 594, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.26441v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 616, \"height\": 314, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.26441v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 626, \"height\": 277, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.26441v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1263, \"height\": 755, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.26441v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1188, \"height\": 227, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.26441v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1249, \"height\": 341, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.26441v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1270, \"height\": 592, \"label\": \"Table\"}]"
motivation: 针对现有弱监督时序定位仅利用粗粒度视频-查询对齐与复杂提案选择，忽略了细粒度帧词匹配和不确定关系建模的问题。
method: 将视频帧和查询词视为博弈玩家，采用多元合作博弈论评估其合作对跨模态相似度的贡献，直接学习帧级得分进行时刻定位。
result: 在Charades-STA和ActivityNet Caption数据集上，本文方法性能大幅超越现有方案。
conclusion: 从博弈角度重新思考弱监督视频时序定位，通过细粒度合作建模避免了提案依赖，有望拓展多模态理解新思路。
---

## 摘要
本文探讨了极具挑战的弱监督视频时序定位任务。现有方法通常基于时刻提议选择框架，利用对比学习和重构范式对预定义的时刻提议进行评分。尽管这些方法已取得显著进展，但我们认为其当前框架忽视了两个不可或缺的问题：1）粗粒度的跨模态学习：以往方法仅捕捉视频级别与查询的全局对齐，未能建模视频帧与查询词之间的细粒度一致性，从而无法准确定位时刻边界。2）复杂的时刻提议：其性能严重依赖于提议的质量，而提议的选择不仅耗时且复杂。为此，本文首次尝试从新颖的博弈视角处理这一任务，有效学习每对视觉-语言之间的不确定关系，以不同粒度和灵活组合实现多层次的跨模态交互。具体而言，我们创新性地将每个视频帧和查询词建模为博弈参与者，并利用多元合作博弈理论学习它们对跨模态相似度得分的贡献。通过博弈论交互量化联盟内帧-词合作的趋势，我们能够评估帧与词之间所有不确定但可能存在的对应关系。最后，我们不再使用时刻提议，而是利用学习到的查询引导的帧级得分来更好地实现时刻定位。实验表明，我们的方法在Charades-STA和ActivityNet Caption数据集上均取得了优越的性能。

## Abstract
This paper addresses the challenging task of weakly-supervised video temporal grounding. Existing approaches are generally based on the moment proposal selection framework that utilizes contrastive learning and reconstruction paradigm for scoring the pre-defined moment proposals. Although they have achieved significant progress, we argue that their current frameworks have overlooked two indispensable issues: 1) Coarse-grained cross-modal learning: previous methods solely capture the global video-level alignment with the query, failing to model the detailed consistency between video frames and query words for accurately grounding the moment boundaries. 2) Complex moment proposals: their performance severely relies on the quality of proposals, which are also time-consuming and complicated for selection. To this end, in this paper, we make the first attempt to tackle this task from a novel game perspective, which effectively learns the uncertain relationship between each vision-language pair with diverse granularity and flexible combination for multi-level cross-modal interaction.Specifically, we creatively model each video frame and query word as game players with multivariate cooperative game theory to learn their contribution to the cross-modal similarity score. By quantifying the trend of frame-word cooperation within a coalition via the game-theoretic interaction, we are able to value all uncertain but possible correspondence between frames and words. Finally, instead of using moment proposals, we utilize the learned query-guided frame-wise scores for better moment localization.Experiments show that our method achieves superior performance on both Charades-STA and ActivityNet Caption datasets.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将基于您提供的论文内容，以中文 Markdown 的形式，对该论文进行结构化、深入、客观的总结。

### **1. 论文的核心问题与整体含义**

-   **核心研究问题**：本文聚焦于**弱监督视频时序定位**任务，即在仅提供视频级别的“视频-查询”配对标注（无帧级别标注）的情况下，根据一段自然语言查询，在一段未剪辑视频中定位出对应事件的起始和结束时刻。
-   **研究动机与背景**：
    *   **现有方案局限**：当前的主流方法普遍采用基于 **时刻提议的框架**，先生成大量候选视频片段，再与查询文本进行匹配打分和选择。
    *   **两个关键缺陷**：
        1.  **粗粒度跨模态学习**：仅进行片段级别的对齐，忽略了**视频帧与查询词之间细粒度的语义一致性**，导致无法精确定位边界。
        2.  **依赖复杂时刻提议**：模型的性能严重受限于**预定义提议的质量**，而密集提议的生成和评估过程复杂且耗时。
    *   **整体含义**：本文旨在**从全新的博弈论视角**解决WS-VTG任务，绕过传统方法中对粗粒度对齐和复杂提议的依赖，通过学习细粒度、不确定的帧-词对应关系，实现更精确、更高效的时刻定位。

### **2. 论文提出的方法论**

-   **核心思想**：将**视频帧和查询词建模为合作博弈中的参与者**。当语义相关的帧和词形成联盟时，它们会为最终的跨模态相似度得分带来额外增益。通过博弈论交互指数来量化和学习这些**细粒度、不确定的对应关系**，从而生成帧级的查询相关度得分，直接用于定位，无需预定义提议。
-   **关键技术细节与流程**：
    1.  **自模态语义增强**：
        *   **视觉自模态博弈**：将视频帧视为玩家，利用**Banzhaf值**计算每一帧对视频整体语义的加权边际贡献，用于增强帧的上下文表征。
        *   **语言自模态博弈**：同理，将查询词视为玩家，计算每个词的边际贡献，增强词的语境表征。
        *   **软监督**：为解决无监督信号和计算成本高的问题，引入一个可学习的“头”（Header）网络，通过**KL散度损失**去模仿和逼近Banzhaf值的分布，在推理时可替代复杂的博弈计算。
        *   **公式体现**：最终增强后的帧特征 \( \tilde{v}_i \) 聚合了其加入各联盟 \( S \) 所带来的贡献 `Softmax[g(S ∪ {v_i}) − g(S)] · (S ∪ {v_i})`。
    2.  **跨模态多层次博弈交互对齐**：
        *   **跨模态博弈**：将增强后的帧 \( \tilde{v}_i \) 和词 \( \tilde{q}_j \) 作为玩家，利用**交互指数 \( I_B(\{\tilde{v}_i, \tilde{q}_j\}) \)** 来衡量它们结成联盟相比单独工作时带来的**额外收益**。这个值量化了帧与词之间的语义紧密度。
        *   **多层次交互**：为捕捉更丰富的语义，将此博弈交互扩展到**词-帧、短语-帧、句子-帧**三个层级。
            *   **公式核心**：\( I_B(\{\tilde{v}_i, \tilde{q}_j\}) = \frac{1}{2^{n-2}} \sum_{S \subseteq \mathcal{P}\backslash\{\tilde{v}_i, \tilde{q}_j\}} [g(S \cup \{\tilde{v}_i, \tilde{q}_j\}) + g(S) - g(S \cup \{\tilde{v}_i\}) - g(S \cup \{\tilde{q}_j\})] \)
            *   **解释**：该公式量化了玩家 \( \tilde{v}_i \) 和 \( \tilde{q}_j \) 结合时，相对于它们分别加入联盟S所带来的非加性增益（协同效应）。
        *   **软监督对齐**：同样设计多层次对齐头，通过KL散度损失学习并预测交互指数矩阵。
    3.  **时刻定位**：
        *   **帧级得分计算**：融合词、短语、句子三个层级的帧交互得分，得到每个帧的最终查询匹配度 `m_i`。
        *   **视频级匹配**：通过句子级相似度得分作为权重，对所有帧的匹配度进行加权求和，得到视频-查询匹配得分 `φ_c`，用于视频级的对比学习。
        *   **边界确定**：推理时，直接利用帧级得分图，从得分最高帧开始向两侧扩展，当候选帧得分与当前边界帧得分比率高于阈值时，将其纳入预测时刻。

### **3. 实验设计**

-   **数据集与场景**：
    *   **Charades-STA**：室内活动视频，包含6672个视频和16128个查询-视频对。平均视频时长29.76秒。
    *   **ActivityNet Caption**：开放域活动视频，包含20000个视频和100000个查询。平均视频时长更长，内容更复杂。
-   **评价指标**：采用“R@n, IoU=m”，即预测的Top-n个时刻中，与真值的交互比（IoU）大于阈值m的比例。
-   **对比方法**：
    *   **全监督方法**：CTRL， 2DTAN， DRN。
    *   **弱监督方法**：SCN, WSTAN, ICVC, MARN, CRM, CNM, ACN, VCA, LCNet, CPL, DM2 等。本文与大量SOTA方法进行了全面对比。

### **4. 资源与算力**

-   **文中未明确说明**使用的GPU型号和具体数量。
-   **训练配置**：
    *   优化器：Adam
    *   学习率：3 × 10^-4，并采用线性衰减和梯度裁剪（1.0）。
    *   批次大小（Batch Size）：16
    *   总训练轮数（Epochs）：100，并采用早停（early stopping）策略。
-   **复杂度对比**：论文在**表3**中提供了与LCNet、CPL等方法的**速度（秒/样本）和GPU显存占用（MB）** 对比。结果显示，本方法虽然训练时因采样博弈样本而消耗较多资源（0.94s， 5237MB），但推理阶段由于直接使用“头”网络（0.10s， 2058MB），速度和内存效率均优于对比方法。

### **5. 实验数量与充分性**

-   **实验数量**：实验设计**较为充分和全面**，大致包含以下几大类：
    1.  **主实验结果**：在两个标准数据集（Charades-STA， ActivityNet Caption）上与大量全监督和弱监督SOTA方法进行了详尽的性能对比（表1）。
    2.  **交叉数据集泛化实验**：在一个数据集训练，在另一个上测试，以评估模型的泛化能力（表2）。
    3.  **消融研究**：系统性地分析了各核心组件的贡献，包括：
        *   不同自模态博弈（视频、文本）的作用。
        *   不同层次的跨模态博弈（词级、短语级、句子级）的作用。
        *   博弈头结构（线性层、卷积、自注意力）的影响。
        *   软监督损失函数（无、MSE、交叉熵、KLD）的效果。
        *   重构损失的必要性。
        *   Banzhaf和Shapley两种博弈交互指数的对比。
    4.  **定性分析**：提供了可视化案例，展示帧-词对齐和边界预测效果（图3）。
    5.  **效率分析**：对比了训练和推理阶段的时间和内存成本（表3）。
-   **公平性与客观性**：实验遵循了领域内的标准设置（如特征提取器、评价指标），对比方法涵盖了最新的研究成果，消融实验对比清晰，整体设计较为规范、客观。

### **6. 论文的主要结论与发现**

-   本文提出的博弈论框架可以有效解决弱监督视频时序定位任务中细粒度对齐缺失的问题。
-   通过将帧-词交互建模为合作博弈，方法能够成功**评估不确定的跨模态对应关系**，从而**绕过对复杂时刻提议的依赖**。
-   多层次（词、短语、句子）博弈交互的引入，为模型提供了**从局部到全局的全面语义理解**，显著提升了接地性能。
-   在Charades-STA和ActivityNet Caption两个权威数据集上，该方法**全面超越了现有的弱监督方法，甚至优于部分全监督方法**，证明了其有效性。

### **7. 优点**

-   **创新性视角**：首次将**合作博弈论**成功应用于弱监督视频时序定位，为跨模态对齐提供了一种全新的、可解释的建模思路。
-   **方法设计精巧**：
    *   **无提案的优雅框架**：通过帧级得分直接定位，避免了提议生成和选择的复杂流程。
    *   **细粒度建模**：能够捕捉到帧与词之间乃至短语、句子层级的精细对应关系，这是以往方法所欠缺的。
    *   **高效的推理方案**：通过可学习的“头”网络模拟复杂的博弈计算，使得推理过程既精准又高效。
-   **实验扎实全面**：不仅在主流数据集上取得了SOTA性能，还通过详尽的消融研究、泛化性测试和效率分析，充分验证了各个模块和整体设计的有效性。

### **8. 不足与局限**

-   **训练复杂度高**：尽管推理效率高，但**训练过程相对耗时且占用更多内存**（文中表3数据），需要近似采样（如Banzhaf值采样5500次）来估算博弈交互指数，增加了训练成本。
-   **理论近似风险**：由于精确计算博弈指数是NP-hard问题，文中采用了**基于采样的近似方法**。这种近似虽然被证明误差可控（1.1%），但仍引入了理论和实际效果上的不确定性。
-   **方法和写作细节**：
    *   论文中引用了“Shapley index [ ?]”，似乎存在引用格式问题，可能遗漏了具体文献。
    *   表5中Shapley变体在ActivityNet Caption上的部分数据出现异常（如R@1, IoU=0.5的CE方法结果为40.06），远高于其他配置，可能存在笔误。
-   **应用限制**：论文在标准数据集上进行了验证，但主要针对的是“根据查询定位单一片段”的场景。对于包含**多个目标时刻的复杂查询**或**长时视频**中的定位任务，其效果需要未来进一步探索和确认。

（完）
